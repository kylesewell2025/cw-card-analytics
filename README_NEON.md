# Neon persistence upgrade

Replace your existing `app.py`, `requirements.txt`, and `.gitignore` with the files in this folder.

Create `.streamlit/secrets.toml` locally using `secrets.toml.example` as the template.
Never commit the real secrets file to GitHub.

In Streamlit Community Cloud, add the same TOML block under:
App settings > Secrets
